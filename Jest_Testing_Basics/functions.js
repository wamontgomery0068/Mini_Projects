// Create a variable to contain our functions
const functions = {
    // Arrow Function "ES6"
    add: (num1, num2) => num1 + num2,
    // Normal Function
    add: function(num1, num2){
        return num1 + num2;
    }
}